/**
 * Re-export SignedPrescriptionBundle from the shared-types package.
 * This file exists for backward compatibility with existing imports.
 */
export type { SignedPrescriptionBundle } from '@ultranos/shared-types'
