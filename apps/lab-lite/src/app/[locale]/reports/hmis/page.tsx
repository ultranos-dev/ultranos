// HMIS monthly report generation page — server component shell.
import { HmisReportGenerator } from '@/components/reports/HmisReportGenerator'

export default function HmisReportPage() {
  return <HmisReportGenerator />
}
