'use client'

/**
 * AppHeader — minimal after sidebar adoption.
 * SyncPulse, NotificationBell, and UserDropdown now live in the sidebar.
 * This component is kept as a placeholder for any future top-bar content
 * (e.g., breadcrumbs, contextual actions).
 */
export function AppHeader() {
  return null
}
